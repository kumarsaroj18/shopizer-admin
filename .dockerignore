node_modules
.git
.gitignore
Dockerfile
README.md
LICENSE
# CI download cache — only Dockerfile.ci needs .ci-download/shopizer-admin/dist/, all others exclude it
# (Dockerfile.ci explicitly COPYs from .ci-download/shopizer-admin/dist/ so it is NOT excluded above)